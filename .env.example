# Main configuration
## Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=japanesestudent

## Service Ports
LEARN_SERVICE_PORT=8080
AUTH_SERVICE_PORT=8081
MEDIA_SERVICE_PORT=8082

## Logging
LOG_LEVEL=info

## CORS Configuration
CORS_ALLOWED_ORIGINS=*

## JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_ACCESS_TOKEN_EXPIRY=1h
JWT_REFRESH_TOKEN_EXPIRY=168h

## API Key (for service-to-service authentication)
API_KEY=your-api-key-change-in-production

## Media Service Configuration
MEDIA_BASE_PATH=/app/media
MEDIA_BASE_URL=http://localhost:8082/api/v6

# Redis Configuration (for task-service)
## Redis host (default: localhost)
REDIS_HOST=localhost
## Redis port (default: 6379)
REDIS_PORT=6379
## Redis password (optional)
REDIS_PASSWORD=
## Redis database number (default: 0)
REDIS_DB=0

# SMTP Configuration (for task-service)
## SMTP server host (default: localhost)
SMTP_HOST=localhost
## SMTP server port (default: 587)
SMTP_PORT=587
## SMTP username (optional, for authenticated SMTP servers)
SMTP_USERNAME=
## SMTP password (optional, for authenticated SMTP servers)
SMTP_PASSWORD=
## From email address (default: noreply@japanesestudent.com)
SMTP_FROM=noreply@japanesestudent.com

## URL of site page or API you want to use as verification url (default: PUBLIC (accessed by users) link to Auth API verification endpoint)
VERIFICATION_URL=http://localhost:8081/api/v6/auth/verify-email

## URL to Task microservice endpoint for immediate task creation (default: INNER (meaning between services) link to our Task API immediate task creation)
TASK_BASE_URL=http://task-api:8083/api/v6/tasks/immediate

# Test configuration
## Test database configuration
TEST_DB_HOST=localhost
TEST_DB_PORT=3306
TEST_DB_USER=root
TEST_DB_PASSWORD=password
TEST_DB_NAME=japanesestudent_test

## Test JWT Configuration
TEST_JWT_SECRET=test-secret-key
TEST_JWT_ACCESS_TOKEN_EXPIRY=1h
TEST_JWT_REFRESH_TOKEN_EXPIRY=168h

## Redis host (default: localhost)
TEST_REDIS_HOST=localhost
## Redis port (default: 6379)
TEST_REDIS_PORT=6379
## Redis password (optional)
TEST_REDIS_PASSWORD=
## Redis database number (default: 0)
TEST_REDIS_DB=0

# Test SMTP Configuration (for task-service)
## SMTP server host (default: localhost)
TEST_SMTP_HOST=localhost
## SMTP server port (default: 587)
TEST_SMTP_PORT=587
## SMTP username (optional, for authenticated SMTP servers)
TEST_SMTP_USERNAME=
## SMTP password (optional, for authenticated SMTP servers)
TEST_SMTP_PASSWORD=
## From email address (default: noreply@japanesestudent.com)
TEST_SMTP_FROM=noreply@japanesestudent.com